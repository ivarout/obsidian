## geometry nodes for path

- [ ] Check if separate detections work with new geometry nodes
- [ ] check what type rhode and schwarz uses for drone detections, and other companies all points on path whose birthframe is smaller than current frame
- [x] at index where birthframe is smaller than current frame, but next index is larger than current frame, interpolate position (or if next idx is 0, clamp to end pos). What happens when idx is out of bounds? Probably becomes zero, which is good.
- [x] draw drone instance at interpolated position.
- [x] draw extra line up to the interpolated position (if we have a next position.)
- [x] set position keyframes on path object, such that child of consteaint works
- [x] in geometry nodes, we subtract the detections location from vertex locatoon (which stores world location)

## start new log
Have function in ui to start new log. This will close and finish cureent log, and open a new one, where we'll store registration msg for all currently active nodes, but nothing else. 

# TIE 25 05 13

- ice breaker 1700-2000
