#todo 

make the shared partition exfat. We aren't able to use bitlocker fully on linux anyways, so encryption is not really an option here.

Shrink the shared partition a bit, 1 TB is more than enough.

When putting sensitive stuff like passwords on it, put it in a password protected archive.